from setuptools import setup, find_packages

setup(
    name="unitree_lerobot",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # 添加必要的依赖
    ],
    python_requires=">=3.8",
)
